<UserControl x:Class="LeakDetectSystem_MVVM.Views.Main.Controls.StationGroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:LeakDetectSystem_MVVM.ViewModels"
             xmlns:controls="clr-namespace:LeakDetectSystem_MVVM.Views.Main.Controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:StationGroupViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="560" d:DesignWidth="600">

    <!--
        StationGroupView: 설정 완료된 카메라 수만큼 스테이션을 동적으로 표시
        - 1개: 1열
        - 2~4개: 2열 (UniformGrid Columns 바인딩)
    -->
    <ItemsControl ItemsSource="{Binding Stations}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <UniformGrid Columns="{Binding GridColumns}" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate DataType="{x:Type vm:StationCardViewModel}">
                <controls:StationCardView />
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
