<dialogs:DialogWindowBase x:Class="LeakDetectSystem_MVVM.Views.Dialogs.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dialogs="clr-namespace:LeakDetectSystem_MVVM.Views.Dialogs"
        Title="{Binding Title}"
        Height="200"
        Width="420"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource SurfaceBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Title}"
                   Style="{StaticResource DialogTitleTextStyle}"/>

        <Border Grid.Row="1" Style="{StaticResource DialogSectionBorderStyle}">
            <StackPanel>
                <TextBlock Text="{Binding Message}"
                           TextWrapping="Wrap"
                           Style="{StaticResource DialogLabelTextStyle}"/>

                <ProgressBar Height="18"
                             Margin="0,12,0,0"
                             Minimum="0"
                             Maximum="100"
                             Value="{Binding ProgressValue}"
                             IsIndeterminate="{Binding IsIndeterminate}"/>

                <TextBlock Margin="0,8,0,0"
                           HorizontalAlignment="Right"
                           Text="{Binding ProgressDisplay}"
                           Style="{StaticResource DialogLabelTextStyle}"/>
            </StackPanel>
        </Border>
    </Grid>
</dialogs:DialogWindowBase>
